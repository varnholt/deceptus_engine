[project]
name = "tmx-lint"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = []

[project.scripts]
tmx-lint = "tmx_lint:main"

[tool.uv]
managed = true

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/tmx_lint"]
