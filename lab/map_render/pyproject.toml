[project]
name = "map-render"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "pytest>=8.0",
    "pillow>=10.0",
    "numpy>=1.26",
    "pywin32>=312",
]

[tool.pytest.ini_options]
testpaths = ["."]
addopts = "-v -s"
