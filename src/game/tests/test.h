#pragma once

/// \brief hosts ad-hoc debugging and math test helpers for development builds.
class Test
{
public:
   /// \brief constructs the test harness object.
   Test();

   /// \brief runs the path interpolation debug output routine.
   void testInterpolation();
};
