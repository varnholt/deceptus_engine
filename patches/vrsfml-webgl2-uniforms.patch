diff --git a/include/SFML/Graphics/DefaultShader.hpp b/include/SFML/Graphics/DefaultShader.hpp
index bf38eedba..334e2cde2 100644
--- a/include/SFML/Graphics/DefaultShader.hpp
+++ b/include/SFML/Graphics/DefaultShader.hpp
@@ -70,6 +70,34 @@ struct DefaultShader
     /// - `vec2 sf_v_texCoord`: Normalized texture coordinates
     ///
     ////////////////////////////////////////////////////////////
+#ifdef __EMSCRIPTEN__
+    // GLSL ES 3.00 (WebGL2) does not allow layout(location=N) on uniforms.
+    // Vertex attributes and fragment outputs keep their location qualifiers.
+    static inline constexpr const char* srcVertex = R"glsl(
+
+uniform vec3 sf_u_mvpRow0;
+uniform vec3 sf_u_mvpRow1;
+uniform sampler2D sf_u_texture;
+uniform vec2 sf_u_invTextureSize;
+
+layout(location = 0) in vec2 sf_a_position;
+layout(location = 1) in vec4 sf_a_color;
+layout(location = 2) in vec2 sf_a_texCoord;
+
+out vec4 sf_v_color;
+out vec2 sf_v_texCoord;
+
+void main()
+{
+    vec3 pos = vec3(sf_a_position, 1.0);
+
+    gl_Position = vec4(dot(sf_u_mvpRow0, pos), dot(sf_u_mvpRow1, pos), 0.0, 1.0);
+    sf_v_color = sf_a_color;
+    sf_v_texCoord = sf_a_texCoord * sf_u_invTextureSize;
+}
+
+)glsl";
+#else
     static inline constexpr const char* srcVertex = R"glsl(

 layout(location = 0) uniform vec3 sf_u_mvpRow0;
@@ -94,6 +122,7 @@ void main()
 }

 )glsl";
+#endif

     ////////////////////////////////////////////////////////////
     /// \brief GLSL source code for the built-in default fragment shader
@@ -113,6 +142,23 @@ void main()
     /// - `vec4 sf_fragColor`: Final color of the fragment
     ///
     ////////////////////////////////////////////////////////////
+#ifdef __EMSCRIPTEN__
+    static inline constexpr const char* srcFragment = R"glsl(
+
+uniform sampler2D sf_u_texture;
+
+in vec4 sf_v_color;
+in vec2 sf_v_texCoord;
+
+layout(location = 0) out vec4 sf_fragColor;
+
+void main()
+{
+    sf_fragColor = sf_v_color * texture(sf_u_texture, sf_v_texCoord);
+}
+
+)glsl";
+#else
     static inline constexpr const char* srcFragment = R"glsl(

 layout(location = 2) uniform sampler2D sf_u_texture;
@@ -128,6 +174,7 @@ void main()
 }

 )glsl";
+#endif

     ////////////////////////////////////////////////////////////
     /// \brief Create an `sf::Shader` instance from the default shader sources
diff --git a/src/SFML/Graphics/RenderTarget.cpp b/src/SFML/Graphics/RenderTarget.cpp
index 0b94fad6d..9187e4b0b 100644
--- a/src/SFML/Graphics/RenderTarget.cpp
+++ b/src/SFML/Graphics/RenderTarget.cpp
@@ -1449,13 +1449,21 @@ void RenderTarget::setupDrawMVP(const Transform& renderStatesTransform,
     if (uploadMVPRow0)
     {
         const float mvpRow0[]{trsfm.a00, trsfm.a01, trsfm.a02};
+#ifdef __EMSCRIPTEN__
+        glCheck(glUniform3fv(glGetUniformLocation(m_impl->cache.lastProgramId, "sf_u_mvpRow0"), /* count */ 1, mvpRow0));
+#else
         glCheck(glUniform3fv(/* location */ 0u, /* count */ 1, mvpRow0)); // `sf_u_mvpRow0`
+#endif
     }

     if (uploadMVPRow1)
     {
         const float mvpRow1[]{trsfm.a10, trsfm.a11, trsfm.a12};
+#ifdef __EMSCRIPTEN__
+        glCheck(glUniform3fv(glGetUniformLocation(m_impl->cache.lastProgramId, "sf_u_mvpRow1"), /* count */ 1, mvpRow1));
+#else
         glCheck(glUniform3fv(/* location */ 1u, /* count */ 1, mvpRow1)); // `sf_u_mvpRow1`
+#endif
     }
 }

@@ -1488,7 +1496,11 @@ void RenderTarget::setupDrawTexture(const RenderStates& states, const bool shade
     if ((mustApplyTexture || shaderChanged) && uploadInvTextureSizeUniform)
     {
         const auto invTexSize = 1.f / usedTexture.getSize().toVec2f();
+#ifdef __EMSCRIPTEN__
+        glCheck(glUniform2f(glGetUniformLocation(m_impl->cache.lastProgramId, "sf_u_invTextureSize"), invTexSize.x, invTexSize.y));
+#else
         glCheck(glUniform2f(/* location */ 3u, invTexSize.x, invTexSize.y));
+#endif
     }
 }

